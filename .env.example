# ── PredEdge 邮件通知(scripts/scan-notify.ts / scripts/mailer.ts)──
# 复制为 .env.local 并填入真实值;.env / .env.local 已被 .gitignore 忽略。

# SMTP 服务器(默认 163;可换任意 SMTP 服务商)
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_SECURE=true

# SMTP 登录邮箱。163 要求 From 地址必须等于该账号,mailer 会自动处理。
MAIL_USER=your_account@163.com

# SMTP 授权码(不是登录密码)。163: 网页设置 → POP3/SMTP/IMAP → 开启服务并生成授权码。
# 绝不要把真实授权码提交进仓库。
MAIL_AUTH_CODE=your_smtp_auth_code_placeholder

# 收件人,默认等于 MAIL_USER,可留空
MAIL_TO=recipient@example.com

# 发件人显示名,默认 "PredEdge Scanner"
MAIL_FROM_NAME=PredEdge Scanner

# ── 链上 RPC(scripts/chain-watch.ts / lib/polymarket)──
# 逗号分隔,按优先级排列。留空则用代码内默认列表。
# 注意:走 Clash 代理的部署要保证这些域名直连(run-cron.sh 会自动把它们加进 NO_PROXY)。
# ONCHAIN_RPC_URLS=https://polygon-bor-rpc.publicnode.com,https://polygon-pokt.nodies.app,https://gateway.tenderly.co/public/polygon,https://1rpc.io/matic

# ── WSL 部署代理(run-cron.sh)──
# Clash Verge 在 Windows 宿主机上的混合端口;留空则全部直连。
# PROXY_PORT=7897

# ── healthchecks.io 死人开关(run-cron.sh 成功后 ping;可选但强烈建议)──
# 注册 healthchecks.io(免费 20 个 check)→ 建两个 check → 把 ping URL 填这里:
#   chain-watch: Period 3 分钟 / Grace 15 分钟
#   scan-notify: Period 30 分钟 / Grace 2 小时
# HC_PING_CHAIN_WATCH=https://hc-ping.com/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# HC_PING_SCAN_NOTIFY=https://hc-ping.com/yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy
